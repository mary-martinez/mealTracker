// import functions and grab DOM elements

const form = document.getElementById('add-ingredient');

// let state
const ingredients = [];
// set event listeners 
// get user input
// use user input to update state 
// update DOM to reflect the new state
form.addEventListener('click', (e) => {
    e.preventDefault();
    // Step 1 - add code to track each submission to the ingredients
    const ingredient = new FormData.get(form));
    // Create an object to "model" you statistic like the following:
    // { player: 'Bob', points: 2 }

});